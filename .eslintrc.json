{
  "extends": ["next/core-web-vitals"],
  "overrides": [
    {
      "files": ["src/app/**/*.{ts,tsx}", "src/components/**/*.{ts,tsx}"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "@/db/client",
                "message": "src/db/client is server-only; import a domain service instead of the database client directly from UI code."
              },
              {
                "name": "@/db/tenant",
                "message": "src/db/tenant is server-only; import a domain service instead of opening a tenant-scoped transaction directly from UI code."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["src/domain/**/*.ts"],
      "rules": {
        "no-restricted-syntax": [
          "error",
          {
            "selector": "CallExpression[callee.name='parseFloat']",
            "message": "Never parse monetary amounts with parseFloat; use Money.of()."
          }
        ]
      }
    }
  ]
}
